


<div class="container">
    <div class="text-center">
        <button class="btn btn-dark filter-buttton" data-filter="all">ALL</button>
        @foreach (var item in collection)
        {
            <button class="btn btn-outline-secondary filter-buttton" data-filter="@item.Name.Replace(' ','_')">
                @item.Name
            </button>

        }
    </div>
    <br />
    <div class="row">
        @foreach (var item in collection)
        {
            //Display ALl Books
        }
    </div>
</div>